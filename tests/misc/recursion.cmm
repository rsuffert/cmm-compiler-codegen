int res;

func int add(int a, int b) {
    int res;

    if (b == 0) {
        res = a;
    } else {
        res = add(++a, --b);
    }

    return res;
}

void main() {
    res = add(3, 2);
    write("res: ", res);
}